== Rails opener bundle

This bundle works like a standard 'Go to File...' but separates rails specific directories.

=== Installation

To install the bundle you should do following steps:

  cd ~/Library/Application\ Support/TextMate/Bundles/
  git clone git://github.com/lunich/Rails-opener-Bundle.git

